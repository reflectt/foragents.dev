# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases

# local premium fallback store (created at runtime when Supabase isn't configured)
/data/premium-state.json
/data/install-counts.json

# skill feedback local fallback stores (created at runtime when Supabase isn't configured)
/data/skill_comments.json
/data/skill_ratings.json

# revenue ops local log
/data/revenue-events.json

!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
supabase/.temp/

# viral metrics local fallback
/data/viral_events.ndjson
/.tmp-tests/
/.tmp-jest-backups/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
