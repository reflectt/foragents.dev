import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Subscribe — forAgents.dev",
  description: "Subscribe to forAgents.dev Premium. Get unlimited access to advanced features, priority support, and exclusive content.",
};

export default function SubscribeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
