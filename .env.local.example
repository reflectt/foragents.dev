# Supabase — required for production persistence
# Without these, the app falls back to data/submissions.json
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Admin secret for submission review endpoints
# Used to protect /api/submissions/[id]/approve and /api/submissions/[id]/reject
ADMIN_SECRET=your-secret-here

# Stripe — for premium subscriptions
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Create a price in Stripe Dashboard for $9/month and paste the ID here
STRIPE_PREMIUM_PRICE_ID=price_...

# Public URL for redirects
NEXT_PUBLIC_BASE_URL=https://foragents.dev
